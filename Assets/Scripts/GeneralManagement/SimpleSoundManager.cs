using UnityEngine;

public class SimpleSoundManager : MonoBehaviour
{
    [SerializeField] private AudioSource mBackgroundMusicSource;

    [SerializeField] public AudioClip MusicClip;
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
